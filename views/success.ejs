<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 flex items-center justify-center min-h-screen p-6">

  <div class="bg-gray-800 rounded-2xl shadow-2xl p-8 max-w-md w-full text-center">
    <!-- Congratulations Icon -->
    <div class="text-green-400 text-6xl mb-4 animate-bounce">🎉</div>

    <!-- Main Message -->
    <h1 class="text-3xl font-bold text-white mb-4">Congratulations, <%= user.displayName || user.email || 'Player' %>!</h1>
    <p class="text-gray-300 mb-6">
      Your team "<span class="text-orange-400 font-semibold"><%= message %></span>" has been successfully registered for the tournament.
    </p>

    <!-- Email Confirmation -->
    <p class="text-gray-400 mb-6">
      We will send a confirmation email to 
      <span class="text-orange-400 font-medium"><%= user.email || 'your email' %></span>.
    </p>

    <!-- WhatsApp CTA -->
    <a href="https://chat.whatsapp.com/BqQv8mVaw4ADWNddhfa0dT?mode=ems_share_t" target="_blank" class="inline-block bg-orange-500 hover:bg-orange-600 text-white font-semibold px-6 py-3 rounded-lg transition duration-300">
      Join WhatsApp Group
    </a>

    <!-- Optional Back Home -->
    <div class="mt-6">
      <a href="/" class="text-gray-400 hover:text-white transition">← Back to Home</a>
    </div>
  </div>

</body>
</html>
