<nav class="fixed top-0 left-0 right-0 z-50 glass border-b border-gray-800">
  <div class="max-w-6xl mx-auto px-4 py-3 flex items-center justify-between text-white">
    <a href="/" class="flex items-center gap-2">
      <img src="fflogo.png" alt="Free Fire Tournament Logo" class="h-16">
    </a>
    <div class="flex items-center gap-3">
      <% if (user) { %>
        <span class="hidden sm:inline text-sm text-gray-300">Hi, <%= user.displayName || (user.email || 'Player') %></span>
        <a href="/logout" class="bg-gray-800 hover:bg-gray-700 text-white px-4 py-2 rounded-lg">Logout</a>
      <% } else { %>
        <a href="/auth/google" class="bg-orange-500 hover:bg-orange-600 text-white px-4 py-2 rounded-lg">Login with Google</a>
      <% } %>
    </div>
  </div>
</nav>

